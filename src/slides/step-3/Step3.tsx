import { SectionTitle, Slide } from '../../components';

export const Step3 = () => (
  <Slide>
    <SectionTitle
      title="Event loop"
      subtitle="How does asynchrony work in the browser?"
    />
  </Slide>
);
